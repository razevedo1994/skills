# Task Manager App

A simple personal task manager CLI built with Python.

## Goals

- Users can create tasks with a title and optional due date
- Tasks can be marked as complete
- Passwords are hashed with bcrypt before being stored
- The app sends an email notification when a task's due date is reached

## Tech Stack

- Python 3.11
- SQLite for storage
- Click for CLI interface
